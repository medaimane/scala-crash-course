object Basics {
  def main(args: Array[String]) {

    // variables
    // val variables are able to changed
    var scalaHelloWorld = "Hello From Scala World"
    
    // val variables are not able to changed
    val myName = "MedAimane"
    val myCity = "Tetouan"

    println(scalaHelloWorld);
    println(myName);
    println(myCity)
  }
}
